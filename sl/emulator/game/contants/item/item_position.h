#pragma once

namespace sunlight
{
    ENUM_CLASS(ItemPositionType, int32_t,
        (Inventory)
        (Equipment)
        (QuickSlot)
        (Pick)
        (Vendor)
        (Mix)

        (Count)
    )
}
